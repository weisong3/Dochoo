package com.chc.found;

public class FoundConstants {
	private FoundConstants() {
		throw new AssertionError("This constants class cannot be instantiated");
	}
	
	/**
	 * bundle key for main activity. When main activity launches,
	 * it uses this bundle key to obtain info for which the 
	 * corresponding tab should be shown in serialized extra
	 */
	public static final String BUNDLE_KEY_ENTRANCE = "main entrance";
	
}
